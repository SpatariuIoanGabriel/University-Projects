# Pseudoprime Base Finder

This tool is designed to identify all bases `b` with respect to which a composite odd number `n` is pseudoprime, leveraging the repeated squaring modular exponentiation method.
